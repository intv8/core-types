/**
 * This file exports the Month enum.
 *
 * @copyright 2022 integer11. All rights reserved. MIT license.
 */

/** Specifies the zero-indexed months of the year. */
export enum Month {
  /** Zero-indexed January. */
  January = 0,

  /** Zero-indexed February. */
  February = 1,

  /** Zero-indexed March. */
  March = 2,

  /** Zero-indexed April. */
  April = 3,

  /** Zero-indexed May. */
  May = 4,

  /** Zero-indexed June. */
  June = 5,

  /** Zero-indexed July. */
  July = 6,

  /** Zero-indexed August. */
  August = 7,

  /** Zero-indexed September. */
  September = 8,

  /** Zero-indexed October. */
  October = 9,

  /** Zero-indexed November. */
  November = 10,

  /** Zero-indexed December. */
  December = 11,
}
