/**
 * This file exports the InputKeyCode enum.
 *
 * @copyright 2022 integer11. All rights reserved. MIT license.
 */

/** Specifies the input key codes. */
export enum InputKeyCode {
  /** The `A` key. */
  A = 65,

  /** The `B` key. */
  B = 66,

  /** The `C` key. */
  C = 67,

  /** The `D` key. */
  D = 68,

  /** The `E` key. */
  E = 69,

  /** The `F` key. */
  F = 70,

  /** The `G` key. */
  G = 71,

  /** The `H` key. */
  H = 72,

  /** The `I` key. */
  I = 73,

  /** The `J` key. */
  J = 74,

  /** The `K` key. */
  K = 75,

  /** The `L` key. */
  L = 76,

  /** The `M` key. */
  M = 77,

  /** The `N` key. */
  N = 78,

  /** The `O` key. */
  O = 79,

  /** The `P` key. */
  P = 80,

  /** The `Q` key. */
  Q = 81,

  /** The `R` key. */
  R = 82,

  /** The `S` key. */
  S = 83,

  /** The `T` key. */
  T = 84,

  /** The `U` key. */
  U = 85,

  /** The `V` key. */
  V = 86,

  /** The `W` key. */
  W = 87,

  /** The `X` key. */
  X = 88,

  /** The `Y` key. */
  Y = 89,

  /** The `Z` key. */
  Z = 90,

  /** The top-row `0` or `(` key. */
  Zero = 48,

  /** The top-row `1` or `!` key. */
  One = 49,

  /** The top-row `2` or `@` key. */
  Two = 50,

  /** The top-row `3` or `#` key. */
  Three = 51,

  /** The top-row `4` or `$` key. */
  Four = 52,

  /** The top-row `5` or `%` key. */
  Five = 53,

  /** The top-row `6` or `^` key. */
  Six = 54,

  /** The top-row `7` or `&` key. */
  Seven = 55,

  /** The top-row `8` or `*` key. */
  Eight = 56,

  /** The top-row `9` or `(` key. */
  Nine = 57,

  /** The `-` or `_` key. */
  Hyphen = 189,

  /** The `=` or `+` key. */
  Equal = 187,

  /** The tick or `~` key. */
  Tick = 192,

  /** The `[` or `{` key. */
  OpenBracket = 219,

  /** The `]` or `}` key. */
  CloseBracket = 221,

  /** The `\` or `|` key. */
  BackSlash = 220,

  /** The `;` or ` =` key. */
  Semicolon = 186,

  /** The `'` or `"` key. */
  Quote = 222,

  /** The `,` or `<` key. */
  Comma = 188,

  /** The `.` or `>` key. */
  Period = 190,

  /** The `/` or `?` key. */
  Slash = 191,

  /** The `Baskspace` key. */
  Backspace = 8,

  /** The `Tab` key. */
  Tab = 9,

  /** The `Enter` key. */
  Enter = 13,

  /** The Spacebar. */
  Space = 32,

  /** The `Delete` key. */
  Delete = 46,
}
